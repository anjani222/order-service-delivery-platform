services:
  order-service:
    build: .
    image: order-service:local
    ports:
      - "8080:8080"
    environment:
      JAVA_TOOL_OPTIONS: "-XX:MaxRAMPercentage=75.0"
    restart: unless-stopped
